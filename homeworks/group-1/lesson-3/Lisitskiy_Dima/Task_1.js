 //git branch --set-upstream-to=origin/<branch> Lisitskiy_Dima_HW_02
const Num = +promt ();

function getnumstring (Number) {
    if (Number == +Number) return `${Number}`
    return "It isn't a number, try again"
}